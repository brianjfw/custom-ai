// Test utility to verify import aliases work correctly
export const testMessage = "Import aliases are working correctly!";

export function formatMessage(message: string): string {
  return `✅ ${message}`;
}